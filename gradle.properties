org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official

# Gli screenshot si confrontano, non si riscrivono.
#
# Senza questa riga un normale `./gradlew test` rigenererebbe le immagini di
# riferimento invece di verificarle: il test non fallirebbe mai e la rete di
# sicurezza sarebbe finta. Per aggiornare i riferimenti dopo una modifica
# voluta serve un comando esplicito, `./gradlew recordRoborazziDebug`.
roborazzi.test.verify=true
